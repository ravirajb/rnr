# Rewards and Recognition Module

## Set up Instructions: 

1. Install yarn
``` npm install -g yarn ```
2. Run DB Migration
``` yarn db:migrate; yarn db:push ```
3. Start the App in dev mode
``` yarn dev ```
